[package]
name = "rusty"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
rusty-cpp-markers = { path = "../rusty-cpp-markers" }

[lints.rust]
unsafe_code = "deny"
